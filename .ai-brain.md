# horrorchatmessages — .ai-brain.md

## Status
Empty, buildable Gradle skeleton only (Package 3, disaster recovery — see
`B:\Claude\RECOVERY.md`). No source copied in yet. `./gradlew build` verified
clean (0 source files, jar produced and confirmed present).

## Reconstruction base: either side works — recommend the 1.21.1 decompile

Unlike `horroractionbar`/`misplacedambience`/`peripheralfigure`, Package 2's
cross-version diff found **no feature gap** between this mod's two builds —
only mechanical API-porting differences (package renames, `@Mod` constructor
signature, `LevelTickEvent` losing its `Phase` split). Recommend copying in
from:
`B:\Claude\Projekte\_RECOVERY\decompiled\horrorchatmessages-neoforge-1.21.1\`

since it's already on the target loader/MC version — no porting step needed
first. The 1.20.1 decompile
(`horrorchatmessages-forge-1.20.1\`) is available as a same-source
cross-check if anything looks off, not as a separate feature source.

## Known open bug, unverified either way
RECOVERY.md section 5 records a still-open bug at time of loss: the whisper
message fired immediately after join instead of being time-delayed. Nothing
in the Package 2 diff suggests this was fixed differently between versions
(both sides identical) — presumably still present, needs an actual fix
during reconstruction, not just a copy-in.

## Not yet done
- No source copied in.
- No `deploy-combined-test-mods.ps1` yet (tracked at the `HorrorTricks/`
  family level).
